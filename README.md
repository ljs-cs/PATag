# PATag
There are two folder, one includes benchmark file with inserted PATag check code, which requires the support of PA and MTE feature by ARM, the other includes benchmark file with equal latency cycle.

