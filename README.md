# PATag
The nbench-test.tar file includes all test code, the benchmark file is inserted both with PATag check code (requires the support of PA and MTE feature by ARM) for implement design and equal latency cycle for evaluation performance .

